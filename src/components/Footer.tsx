import React from 'react';
import styled from 'styled-components';

const FooterContainer = styled.footer`
  /* Add your styles for the footer */
`;

const Footer = () => {
  return (
    <FooterContainer>
      <p>&copy; University Web App</p>
    </FooterContainer>
  );
};

export default Footer;
